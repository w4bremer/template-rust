schema: "apigear.solution/1.0"
name: demo
version: "0.1"

layers:
  - name: goldenmaster
    inputs:
      - testbed.simple.module.yaml
    output: ../test
    template: ..
    force: true
    features: [ "all" ]
